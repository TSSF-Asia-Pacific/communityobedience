services:
  webserver:
    build:
      dockerfile: Dockerfile
      context: ./
    restart: "always"
    ports:
      - "8080:8080"
    working_dir: /var/www/html
    command: >
      sh -c "
      yarn install &&
      yarn run start
      "
    volumes:
      - ./:/var/www/html
